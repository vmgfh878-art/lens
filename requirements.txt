# Lens reproducibility manifest (Python 3.10.0).
# 주의: ai/preprocessing.py 가 backend collector 를 모듈 최상단에서 import 하므로,
# 운영 모델 재현(학습/추론/평가)에는 아래 세 파일을 모두 설치해야 한다:
#   pip install -r requirements.txt -r backend/requirements.txt -r backend/collector/requirements.txt
# (python-dotenv / supabase 는 backend/requirements.txt, yfinance 는 backend/collector/requirements.txt
#  에 있다. 이 파일 단독 설치만으로는 import 가 실패한다.)
# Truth source for versions: docs/cp224a_venv_freeze.txt (.venv freeze, Python 3.10.0).

# torch is a CUDA build (+cu128 local label) and cannot be pinned via
# PyPI. Install separately:
#   https://pytorch.org/get-started/locally/
# (5060 Ti sm_120 -> cu128, torch>=2.7 recommended).
# Currently verified: torch==2.11.0+cu128 / torchvision==0.26.0+cu128.

# Numeric core
numpy==1.26.4
pandas==2.2.2
scipy==1.15.3
pyarrow==17.0.0  # backend/requirements.txt 와 일치(3개 파일 교차 설치 충돌 방지). parquet I/O 라 모델 계산 불변.

# HPO / plotting (existing pins kept)
optuna==4.8.0
optuna-dashboard==0.20.0
matplotlib==3.10.9

# Note: scikit-learn / statsmodels / arch are NOT used in ai/ (grep
# confirmed 0 import sites). backend/requirements.txt keeps scikit-learn
# and statsmodels for legacy reasons -- not relevant to this dev manifest.
