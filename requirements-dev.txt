# CP222 safety-net dev tooling. Separated from runtime requirements.txt.
pytest==8.3.3
pytest-cov==5.0.0
ruff==0.6.9
mypy==1.11.2
pre-commit==3.8.0
httpx==0.27.0          # FastAPI TestClient dependency (test_api.py uses fastapi.testclient).
pandera==0.20.4        # CP223 dataframe schema snapshot candidate.
# CP223 chose syrupy (PyPI snaptol unavailable). Float tolerance handled
# by normalize_floats() helper in backend/tests/conftest.py.
syrupy==5.3.1          # characterization snapshot (read-path API).
# CP243 — 코드 복잡도 게이트 (maintainability index + cyclomatic complexity).
radon==6.0.1           # MI/CC 측정.
xenon==0.9.3           # radon 기반 복잡도 CI 게이트.
