# CP222 safety-net dev tooling. Separated from runtime requirements.txt.
pytest==8.3.3
pytest-cov==5.0.0
ruff==0.6.9
mypy==1.11.2
pre-commit==3.8.0
httpx==0.27.0          # FastAPI TestClient dependency (test_api.py uses fastapi.testclient).
pandera==0.20.4        # CP223 dataframe schema snapshot candidate.
# ML numeric snapshot libraries -- CP223 will pick one. Listed for reference only:
#   - syrupy  (pytest plugin, serialized snapshot)
#   - snaptol (numeric tolerance snapshot)
# Not installed in CP222. CP223 will add the chosen one.
